[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ascent-load"
version = "1.4.0"
description = "ASCENT-Load — open-source aeroelastic loads-analysis framework with Nastran BDF I/O (ASCENT Lab, Inha University)"
license = "AGPL-3.0-or-later"
license-files = ["../LICENSE", "../NOTICE"]
authors = [{ name = "Eunsuk Lee" }]
classifiers = [
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.22",
    "scipy>=1.9",
    # 인증 설정(.yaml)을 읽는 경로에 필요 — ILC-8/ILC-8T 재현 덱이 YAML이다
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-cov"]
plot = ["matplotlib>=3.5"]
gui = [
    "qtpy>=2.3",
    "pyside6>=6.5",
    "pyvista>=0.43",
    "pyvistaqt>=0.11",
    "matplotlib>=3.5",
]

[project.scripts]
ascent-load = "ascent_load.__main__:main"
ascent-load-gui = "ascent_load.gui.app:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["ascent_load*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
